#ifndef _UTILS_H_
#define _UTILS_H_

#include <iostream>
#include <cstring>
#include <cstdlib>
#include <ctime>
using namespace std;

int ArgPos(char*, int, char**);
void generateArray(int*, long);
void printArray(int*, int);
void swap(int*, int*);

#endif
